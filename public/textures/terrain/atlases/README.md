# Baked terrain atlases

Generated by `npm run bake:terrain-atlases` — do not hand-edit.

Play loads `color.ktx2` + `orm.ktx2` via `loadBakedTerrainAtlases` (`src/world/terrain/loaders/loadBakedTerrainAtlases.ts`).
Per-biome folders under `../` remain bake sources + editor color-only inputs (filename scan).
