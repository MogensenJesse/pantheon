// src/config/visual/editor.ts — map editor sculpt tools (DEV)

export const editor = {
    ridgeSculpt: {
      frequency: 1 / 48,
      octaves: 4,
      lacunarity: 2,
      gain: 0.45,
      strength: 0.06,
      smoothStrength: 0.08,
    },
  } as const;
