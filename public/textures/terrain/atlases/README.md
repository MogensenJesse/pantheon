# Baked terrain atlases

Generated by `npm run bake:terrain-atlases` — do not hand-edit.

Play loads `color.ktx2` + `ao.ktx2` via `loadBakedTerrainAtlases` (`src/world/terrain/loaders/loadBakedTerrainAtlases.ts`).
Per-biome folders under `../` remain bake sources; editor prefers `color.ktx2` (1024 canvas pack if missing).
