// src/world/grass/tsl/tslNode.ts — opaque TSL graph node (Three r184 typings incomplete)

/** Shader graph node from `three/tsl` — use instead of per-file `any` where params cross modules. */
export type TslNode = any;
